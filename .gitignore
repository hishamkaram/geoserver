# Compiled binaries and shared libraries
*.exe
*.so
*.dylib

# Test binaries built with `go test -c`
*.test

# Coverage and profiling output
coverage.out
coverage.html
*.coverprofile
*.prof

# Editor / OS scratch files
.DS_Store
*~
.*.swp
*.orig
*.rej
._*

# Vendored deps (project does not vendor; pattern preserved as safety)
/vendor/

# Claude Code per-machine state (committed config lives in .claude/agents,
# .claude/skills, .claude/commands; only the local settings file is ignored)
.claude/settings.local.json
.claude/.cache/
.claude/scheduled_tasks.lock
